version: '3.3'
services:
  mysql:
    container_name : mysql_symfony
    image: mysql:8.0
    restart: on-failure
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: photodec
      MYSQL_USER: photodec
      MYSQL_PASSWORD: admin
  pma:
    container_name : pma_symfony
    image: phpmyadmin
    restart: on-failure
    ports:
      - '8000:80'
    environment:
      PMA_HOST: mysql
  php:
    container_name : php_symfony
    build:
      context: .
      dockerfile: docker/php/Dockerfile
    volumes:
      - './apps/:/var/www/html'
    ports:
      - '80:80'
      - '443:443'
    restart: on-failure